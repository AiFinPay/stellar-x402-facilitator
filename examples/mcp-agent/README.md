# MCP agent example

Planned end-to-end example in which an MCP client discovers a paid tool, evaluates structured payment requirements, pays through the canonical Stellar x402 flow and invokes the resource.

The example will demonstrate deterministic discovery errors and will not place private signing material in the agent process.
