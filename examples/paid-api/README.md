# Paid HTTP API example

Planned end-to-end example that publishes one paid HTTP resource, registers its discovery metadata, accepts an unmodified canonical client payment and exposes the resulting Stellar transaction evidence.

The example will support testnet by default. Mainnet use will require an explicit profile and safety acknowledgement.
