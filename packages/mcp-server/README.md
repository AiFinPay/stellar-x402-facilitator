# MCP discovery server

Planned Model Context Protocol interface over the normalized Bazaar catalog.

The server will publish versioned JSON Schemas, stable error codes and equivalent resource identifiers across HTTP and MCP discovery. Settlement remains in the canonical facilitator path; this package discovers and describes paid resources.
