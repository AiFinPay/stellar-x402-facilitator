# SDK helpers

Planned TypeScript helpers for registering paid resources, querying discovery and integrating canonical x402 clients with the facilitator.

Helpers will not hide network, asset, fee sponsorship or trust assumptions. Canonical protocol types should be imported or adapted from upstream packages rather than forked silently.
