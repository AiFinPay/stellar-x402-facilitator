# Bazaar discovery package

Planned normalized resource catalog, automatic registration pipeline and Bazaar-compatible search layer for paid HTTP and MCP resources.

The implementation will include provenance, ownership verification, moderation, rate limits and deterministic structured search results. It will not mix secrets or private payment payloads into discovery metadata.
